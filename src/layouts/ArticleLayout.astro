---
interface Props {
  title: string;
  description: string;
  icon: string;
  category: string;
}

const { title, description, icon, category } = Astro.props;
---

<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content={description}>
  <title>{title} - Software Engineering 3.0</title>
  <link rel="stylesheet" href="/software-engineering-3.0/styles/global.css">
</head>
<body>
  <div class="container">
    <header class="article-header">
      <span class="badge badge-{category === 'article' ? 'article' : 'guide'}">
        {category === 'article' ? 'ARTICOLO' : 'GUIDA'}
      </span>
      <h1>{title}</h1>
      <p class="tagline">{description}</p>
    </header>

    <article class="article-content">
      <slot />
    </article>

    <footer>
      <p>Software Engineering 3.0</p>
      <p><a href="/software-engineering-3.0/">← Torna alla Home</a></p>
    </footer>
  </div>
</body>
</html>
